# 이용 범위 (Acceptable Use)

이 문서는 MIT 라이선스를 대체하지 않습니다. 라이선스는 **코드의 사용 조건**을 정하고,
이 문서는 **그 코드로 무엇을 하는 것이 적절한가**에 대한 저장소의 입장을 밝힙니다.

## 이 도구가 하는 일

사용자가 지정한 사이트에서 데이터를 수집해 엑셀로 정리합니다. **어떤 사이트를 대상으로 할지,
수집한 데이터를 어디에 쓸지는 전적으로 사용자가 정합니다.**

> 이 문서는 **도구의 동작**을 정의합니다. 실제로 실행하는 AI 에이전트(Claude·Codex)는
> **자체 판단 기준**을 갖고 있으며, 이 문서가 그것을 대신 약속하지 않습니다.
> 사용자가 진행을 선택했는데 에이전트가 멈추는 경우가 있을 수 있습니다.

## 이 도구가 하지 않는 일

- **CAPTCHA 자동 풀이** — 프로그램으로 CAPTCHA 를 푸는 기능은 제공하지 않습니다
- **자동 접근 차단을 조용히 넘어가기** — 봇 탐지·WAF·CAPTCHA 를 만나면 **자동으로 넘어가지 않고
  사용자에게 한 번 알리고 묻습니다.** 다만 이는 **금지가 아니라 확인**입니다. 사용자가 진행을
  고르면 그대로 진행하며, 근거를 제출받거나 검증하지 않습니다
- **법적 위험을 조용히 넘어가기** — 저작권 침해 목적의 본문 복제·개인정보 대량 수집·명시적으로
  금지된 재배포처럼 위험이 큰 축이 걸리는 요청을 받으면 **어느 축이 왜 걸리는지 구체적으로
  알립니다.** 이 역시 확인이며, 알린 뒤 진행 여부는 사용자가 정합니다 — 요청 자체를 막거나
  근거를 심사하지 않습니다
- **사용자의 법적 권한 심사** — 저자는 사용자가 특정 사이트를 수집할 권한이 있는지 판단할 위치에
  있지 않습니다. 판단의 주체는 사용자입니다

## 사용자의 책임

1. **대상 사이트의 이용약관과 robots.txt 를 확인할 책임**
2. **수집 부담을 상대 서버가 감당할 수 있는 수준으로 유지할 책임** — 이 도구는 요청 간 지연
   하한과 연속 실패 시 자동 중단을 기본 제공하지만, **총 요청 건수 상한은 기본값이 없고
   사용자가 직접 설정해야 합니다**
3. **개인정보가 포함된 데이터의 수집·보관·이용에 대한 책임** — 사이트가 허락해도 그 허락에
   권한이 없는 영역이 있습니다
4. **수집 이후 이용에 대한 책임** — 재배포·경쟁 서비스 구축·학습 데이터 이용은 수집 행위와
   **별개의 법적 평가**를 받습니다. "어떻게 들어갔나" 와 "가져다 뭘 했나" 는 다른 질문입니다

## 기여자 규칙

**자동 접근 차단을 넘어선 방법이 담긴 도메인 프로필은 PR 로 받지 않습니다.**

- 판정은 `scripts/profile_policy.py` 가 프로필의 구조 필드에서 자동으로 합니다
- `.gitignore` 의 배포 화이트리스트는 그 판정으로 생성되므로, 해당 프로필은 **일반 `git add` 로는
  인덱스에 들어가지 않습니다**
- `scripts/test_profile_policy.py` 가 CI 에서 이를 다시 검사합니다

이는 도구의 능력을 제한하는 것이 아닙니다. 그런 프로필은 **로컬에 그대로 남고 정상 동작합니다.**
빠지는 것은 배포뿐입니다. 저작권법 제104조의2 제2항이 겨냥하는 것은 사용이 아니라 **배포**입니다.
이 저장소가 해당 프로필을 배포에서 뺀 것은 그 때문입니다.

우회가 아닌 다른 이유(robots 차단·계약·ToS)로 배포를 원하지 않는 프로필은
`"distribution": "local"` 을 명시하고 `"distribution_reason"` 에 이유를 적으세요.

## 면책

이 소프트웨어는 MIT 라이선스에 따라 **있는 그대로** 제공됩니다. 저자는 이 도구의 사용으로
발생하는 어떠한 법적 결과에 대해서도 책임지지 않습니다.

**저자는 변호사가 아니며, 이 저장소의 어떤 문서도 법률 자문이 아닙니다.**
상업적 이용 전 전문가 확인을 권합니다.
