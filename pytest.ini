[pytest]
# 테스트는 scripts/ 아래에 있다.
testpaths = scripts
# Windows/Codex 제한 환경에서 기본 OS temp(예: C:\Users\...\AppData\Local\Temp) 접근이
# PermissionError 를 내는 경우가 있어, repo-local temp 를 기본으로 쓴다. (.tmp/ 는 .gitignore)
# conftest.py 가 TEMP/TMP/tempfile.tempdir 도 .tmp 로 맞춘다.
addopts = --basetemp=.tmp/pytest
